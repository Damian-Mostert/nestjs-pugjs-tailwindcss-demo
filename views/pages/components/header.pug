header.w-full.flex.items-center.gap-4
  a(href="/")
    img.w-24.h-24(width="100", height="100", src="/assets/logo.png")
  
  div
    each link in links
      a(href=link.url)= link.text
  
  if user
    div
  else
    div
