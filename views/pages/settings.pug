include components/head.pug
body
  include components/header.pug
  main
    .min-h-screen.p-4
    each settingKey in Object.keys(user.settings).filter(s => s !== 'id')
      .mb-4
        div #{settingKey}
        div #{user.settings[settingKey]}
  include components/footer.pug
